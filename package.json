{
  "name": "transcendance",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "install": "npm i frontend && npm i api-gateway && npm i services/ai-opponent && npm i services/live-chat && npm i services/match-server && npm i services/database && npm i services/user-management"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
